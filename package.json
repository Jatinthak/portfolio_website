```json
{
  "name": "portfolio_website",
  "version": "1.0.0",
  "description": "A professional online portfolio to showcase skills and projects",
  "main": "src/backend/app.js",
  "scripts": {
    "start": "node src/backend/app.js",
    "test": "jest",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm run --prefix src/frontend build",
    "build:backend": "npm run --prefix src/backend build",
    "deploy": "docker-compose up -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^1.3.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "rate-limiter-flexible": "^2.3.0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "@babel/preset-react": "^7.12.10",
    "babel-jest": "^27.0.6",
    "jest": "^27.0.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "supertest": "^6.1.3"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
```